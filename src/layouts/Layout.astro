---
import SEO from '../components/SEO.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import AffiliateTracking from '../components/AffiliateTracking.astro';
import '../styles/global.css';

export interface Props {
  title: string;
  description: string;
  canonical?: string;
  ogImage?: string;
  type?: 'website' | 'article';
  schema?: object;
  noindex?: boolean;
  bare?: boolean;
}

const { bare, ...seoProps } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/png" href="/rr-icon.png" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" />
    <SEO {...seoProps} />
    <!-- Google Tag Manager -->
    <script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
    new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
    j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
    'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
    })(window,document,'script','dataLayer','GTM-KCV3GWZ');</script>
    <!-- End Google Tag Manager -->
  </head>
  <body>
    <!-- Google Tag Manager (noscript) -->
    <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-KCV3GWZ"
    height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
    <!-- End Google Tag Manager (noscript) -->
    {bare ? (
      <main class="bare-main">
        <div class="bare-brand">
          <a href="https://redefiningretirement.io" aria-label="Redefining Retirement">
            <img src="/rr-logo.jpg" alt="Redefining Retirement" />
          </a>
        </div>
        <slot />
      </main>
      <Footer hideSocials={true} />
    ) : (
      <Fragment>
        <Header />
        <main>
          <slot />
        </main>
        <Footer />
      </Fragment>
    )}

    <AffiliateTracking />
  </body>
</html>

<style>
  .bare-brand {
    display: flex;
    justify-content: center;
    padding: 1.75rem 1.5rem;
  }
  .bare-brand img {
    height: 34px;
    width: auto;
    display: block;
  }
</style>
